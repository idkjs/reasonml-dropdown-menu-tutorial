{
  "name": "reasonml-dropdown-menu-tutorial",
  "version": "0.1.0",
  "private": true,
  "keywords": [
    "ReasonML",
    "OCaml",
    "Tutorial",
    "DropDownMenu",
    "Rescript"
  ],
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "3.0.1",
    "reason-react": "0.9.1"
  },
  "scripts": {
     "start": "SKIP_PREFLIGHT_CHECK=true FORCE_COLOR=true react-scripts start | cat -n",
    "build": "bsb -clean-world -make-world; react-scripts build",
    "test": "react-scripts test",
    "watch": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "bs-platform": "9.0.2"
  }
}
